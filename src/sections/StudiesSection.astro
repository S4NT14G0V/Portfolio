---
import Studies from '../components/Studies.astro';

const { lang } = Astro.props;

const title = lang?.studies?.title ?? "Titulo de Sección";
const studies = lang?.studies?.list ?? [];
const id = lang?.studies?.id;
---

<section id={id} class="flex flex-col justify-center items-center gap-16 py-16 sm:py-32 w-full px-10 sm:w-1/2 sm:px-0 min-h-screen z-20">
    <p class="text-2xl font-bold text-black/85 dark:text-white/85 z-20 text-center">{title}</p>
    <Studies studies={studies}/>
</section>